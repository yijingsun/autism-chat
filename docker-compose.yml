services:
  autism-chat:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # 挂载知识库目录，方便随时添加资料
      - ./data/knowledge:/app/data/knowledge
      - ./data/faiss_index:/app/data/faiss_index
    restart: unless-stopped
